# My personal site!
Powered by [Melon](https://github.com/Tsunami014/Melon) (which I made)

## Building
Ensure you have `minify` installed, then run `./build.sh`. The resulting files will be in `./out`

You can test e.g. with `python -m http.server -d out/`
